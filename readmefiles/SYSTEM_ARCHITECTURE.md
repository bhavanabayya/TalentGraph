# 🏗️ System Architecture Diagram - Oracle Products & Roles

## Complete Data Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         ORACLE PRODUCTS & ROLES SYSTEM                  │
└─────────────────────────────────────────────────────────────────────────┘

                                DATA LAYER
┌────────────────────────────────────────────────────────────────────────┐
│                                                                        │
│  backend/app/data/roles.json                                          │
│  ┌─────────────────────────────────────────────────────────────────┐  │
│  │ {                                                               │  │
│  │   "product_authors": {                                          │  │
│  │     "Oracle": {                                                 │  │
│  │       "products": {                                             │  │
│  │         "SaaS": { "roles": [7 roles] },                        │  │
│  │         "E-Business Suite": { "roles": [7 roles] },            │  │
│  │         "PeopleSoft": { "roles": [6 roles] },                 │  │
│  │         "JD Edwards": { "roles": [5 roles] }                  │  │
│  │       }                                                         │  │
│  │     }                                                           │  │
│  │   }                                                             │  │
│  │ }                                                               │  │
│  └─────────────────────────────────────────────────────────────────┘  │
│                                                                        │
│  📊 Total Data: 4 Products, 25 Roles                                   │
└────────────────────────────────────────────────────────────────────────┘
                                   ↓
                              API LAYER
┌────────────────────────────────────────────────────────────────────────┐
│  backend/app/routers/job_roles.py                                      │
│                                                                        │
│  ┌──────────────────────────────────────┐                             │
│  │ GET /job-roles/products              │                             │
│  │ Parameters: author='Oracle'          │                             │
│  │ Response: ["SaaS", "E-Business..."]  │                             │
│  └──────────────────────────────────────┘                             │
│                    ↓                                                   │
│  ┌──────────────────────────────────────┐                             │
│  │ GET /job-roles/roles                 │                             │
│  │ Parameters: author, product          │                             │
│  │ Response: [list of roles]            │                             │
│  └──────────────────────────────────────┘                             │
│                                                                        │
│  ✅ Running on: http://127.0.0.1:8000                                 │
└────────────────────────────────────────────────────────────────────────┘
                                   ↓
                        FRONTEND API CLIENT
┌────────────────────────────────────────────────────────────────────────┐
│  react-frontend/src/api/client.ts                                      │
│                                                                        │
│  jobRolesAPI = {                                                       │
│    getProducts: (author) → calls /job-roles/products                  │
│    getRoles: (author, product) → calls /job-roles/roles               │
│  }                                                                      │
│                                                                        │
│  Response Handling:                                                    │
│  - Converts response format if needed                                  │
│  - Returns clean array data                                            │
└────────────────────────────────────────────────────────────────────────┘
                                   ↓
                        REACT COMPONENT LAYER
┌────────────────────────────────────────────────────────────────────────┐
│  react-frontend/src/pages/CandidateDashboard.tsx                       │
│                                                                        │
│  ┌─ Component Mount ──────────────────┐                               │
│  │ fetchAllData() called              │                               │
│  │ jobRolesAPI.getProducts('Oracle')  │                               │
│  │ products[] = [4 items]             │                               │
│  └────────────────────────────────────┘                               │
│         ↓                                                              │
│  ┌─ State Management ──────────────────────────────┐                  │
│  │ State Variables:                                │                  │
│  │ - products = ["SaaS", "E-Business Suite", ...]  │                  │
│  │ - roles = []  (empty initially)                 │                  │
│  │ - formData.product = ""                         │                  │
│  │ - formData.primary_role = ""                    │                  │
│  │ - formData.availability = ""                    │                  │
│  └─────────────────────────────────────────────────┘                  │
│         ↓                                                              │
│  ┌─ Event Handlers ────────────────────────────────┐                  │
│  │ handleLoadProducts(author)                      │                  │
│  │   → Sets products[] state                       │                  │
│  │                                                 │                  │
│  │ handleLoadRoles(author, product)                │                  │
│  │   → Sets roles[] state                          │                  │
│  │                                                 │                  │
│  │ handleUpdateProfile()                           │                  │
│  │   → Saves form data to backend                  │                  │
│  └─────────────────────────────────────────────────┘                  │
│                                                                        │
│  ✅ Running on: http://localhost:3000                                 │
└────────────────────────────────────────────────────────────────────────┘
                                   ↓
                          VIEW LAYER (HTML)
┌────────────────────────────────────────────────────────────────────────┐
│  Candidate Profile Form with Product/Role Focus Section                │
│                                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐ │
│  │ Edit Your Profile                                                │ │
│  │                                                                  │ │
│  │ Product/Role Focus                                              │ │
│  │ ┌──────────────────────────────────────────────────────────┐    │ │
│  │ │ Product Vendor:  ┌─────────────────────┐               │    │ │
│  │ │                  │ Oracle      (gray)   │  ← Disabled   │    │ │
│  │ │                  └─────────────────────┘               │    │ │
│  │ │                                                         │    │ │
│  │ │ Product Type:    ┌─────────────────────┐               │    │ │
│  │ │                  │ [Select Product...▼]│ ← Dropdown    │    │ │
│  │ │                  │ ┌──────────────────┐│               │    │ │
│  │ │                  │ │ SaaS             ││               │    │ │
│  │ │                  │ │ E-Business Suite ││               │    │ │
│  │ │                  │ │ PeopleSoft       ││               │    │ │
│  │ │                  │ │ JD Edwards       ││               │    │ │
│  │ │                  │ └──────────────────┘│               │    │ │
│  │ │                  └─────────────────────┘               │    │ │
│  │ │                                                         │    │ │
│  │ │ Primary Role:    ┌─────────────────────┐ (Appears      │    │ │
│  │ │                  │ [Select Role...▼]   │  when product  │    │ │
│  │ │                  │ ┌──────────────────┐│  selected)    │    │ │
│  │ │                  │ │ Oracle Fusion... ││               │    │ │
│  │ │                  │ │ Oracle EBS...    ││               │    │ │
│  │ │                  │ │ ...more roles... ││               │    │ │
│  │ │                  │ └──────────────────┘│               │    │ │
│  │ │                  └─────────────────────┘               │    │ │
│  │ │                                                         │    │ │
│  │ │ Availability:    ┌─────────────────────┐               │    │ │
│  │ │                  │ [e.g., 2 weeks]     │ ← Text input  │    │ │
│  │ │                  └─────────────────────┘               │    │ │
│  │ │                                                         │    │ │
│  │ │ [Save Profile]                                          │    │ │
│  │ └──────────────────────────────────────────────────────────┘    │ │
│  └──────────────────────────────────────────────────────────────────┘ │
│                                                                        │
│  Candidate sees and interacts with form                                │
└────────────────────────────────────────────────────────────────────────┘
```

---

## Component Interaction Diagram

```
┌──────────────────────────────────────────────────────────────┐
│           CANDIDATE DASHBOARD COMPONENT LIFECYCLE             │
└──────────────────────────────────────────────────────────────┘

1. MOUNT PHASE
   ├── Component mounts
   ├── fetchAllData() called
   ├── jobRolesAPI.getProducts('Oracle')
   ├── API response received
   ├── setProducts([4 products])
   └── Product Type dropdown populated with 4 options

                            ↓

2. DISPLAY PHASE
   ├── User sees form with:
   │   ├── Product Vendor: "Oracle" (disabled)
   │   ├── Product Type: [Select Product...] with 4 options
   │   ├── Primary Role: (hidden, waiting for selection)
   │   └── Availability: [text input]
   └── Form ready for interaction

                            ↓

3. USER SELECTS PRODUCT
   ├── User clicks "Product Type" dropdown
   ├── User selects product (e.g., "SaaS")
   ├── onChange handler fires
   ├── setFormData({ product: "SaaS", primary_role: "" })
   ├── setRoles([]) (clear previous roles)
   └── Primary Role field appears in UI

                            ↓

4. LOAD ROLES PHASE
   ├── User sees "Primary Role" field appear
   ├── User clicks "Primary Role" dropdown
   ├── onFocus handler fires
   ├── handleLoadRoles('Oracle', 'SaaS') called
   ├── jobRolesAPI.getRoles('Oracle', 'SaaS')
   ├── API response received
   ├── setRoles([7 SaaS roles])
   └── Role dropdown populated with 7 options

                            ↓

5. USER SELECTS ROLE
   ├── User clicks role dropdown
   ├── User selects role (e.g., "Oracle Fusion...")
   ├── onChange handler fires
   ├── setFormData({ primary_role: "Oracle Fusion..." })
   └── Form shows selected role

                            ↓

6. USER SETS AVAILABILITY
   ├── User clicks "Availability" text input
   ├── User types custom text (e.g., "2 weeks")
   ├── onChange handler fires
   ├── setFormData({ availability: "2 weeks" })
   └── Form shows custom text

                            ↓

7. SAVE PHASE
   ├── User clicks "Save Profile"
   ├── handleUpdateProfile() called
   ├── FormData = {
   │   product: "SaaS",
   │   primary_role: "Oracle Fusion...",
   │   availability: "2 weeks",
   │   ...other fields...
   │}
   ├── candidateAPI.updateProfile(formData)
   ├── Backend saves to database
   ├── Success message shown
   └── Form data persists

```

---

## Data Flow Visualization

```
┌────────────────────────────────────────────────────────────────┐
│                      USER INTERACTION FLOW                     │
└────────────────────────────────────────────────────────────────┘

START
  ↓
┌─────────────────────────────────────┐
│ Candidate Opens Edit Profile        │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Component Mounts                    │
│ Products Pre-loaded: [4]            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ CHOICE POINT 1: Product Selection   │
│                                     │
│ [SaaS] ──→ 7 roles (next page)     │
│ [EBS] ──→ 7 roles (next page)       │
│ [PS] ──→ 6 roles (next page)        │
│ [JDE] ──→ 5 roles (next page)       │
└─────────────────────────────────────┘
  ↓ (User selects product)
┌─────────────────────────────────────┐
│ CHOICE POINT 2: Role Selection      │
│ (depends on product selected)       │
│                                     │
│ For SaaS:                           │
│ [Fusion Functional] ──→ Form filled │
│ [Fusion Technical] ──→ Form filled  │
│ [OIC Developer] ──→ Form filled     │
│ ...5 more options...                │
└─────────────────────────────────────┘
  ↓ (User selects role)
┌─────────────────────────────────────┐
│ Set Availability (Custom Text)      │
│ [e.g., "2 weeks notice"]            │
└─────────────────────────────────────┘
  ↓ (User types text)
┌─────────────────────────────────────┐
│ Review Form & Save                  │
│ All fields filled:                  │
│ ✓ Product                           │
│ ✓ Role                              │
│ ✓ Availability                      │
└─────────────────────────────────────┘
  ↓ (User clicks Save)
┌─────────────────────────────────────┐
│ Submit to Backend                   │
│ POST /candidates/me                 │
│ With: product, primary_role,        │
│       availability, etc.            │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ Backend Saves to Database           │
│ ✓ Profile updated                   │
│ ✓ Data persists                     │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│ SUCCESS                             │
│ Candidate profile complete          │
│ Product/Role selected               │
│ Availability set                    │
└─────────────────────────────────────┘
END
```

---

## Database Structure

```
┌──────────────────────────────────────────────────────────────┐
│                    DATABASE SCHEMA                           │
└──────────────────────────────────────────────────────────────┘

candidates table:
┌─────────────────────────────────────────┐
│ id (Primary Key)                        │
│ user_id                                 │
│ name                                    │
│ email                                   │
│ location                                │
│ product ────────────────┐               │
│ primary_role ─────────┐ │               │
│ availability ────────┐│ │               │
│ years_experience     ││ │               │
│ ...other fields...   ││ │               │
└─────────────────────────────────────────┘
                       │││
            ┌──────────┘││
            │ [Stores]  ││
            │           ││
            v           vv
        ┌───────────────────────┐
        │  roles.json Data      │
        │ (Read-only reference) │
        │                       │
        │ "SaaS":               │
        │ - 7 roles             │
        │                       │
        │ "E-Business Suite":   │
        │ - 7 roles             │
        │                       │
        │ "PeopleSoft":         │
        │ - 6 roles             │
        │                       │
        │ "JD Edwards":         │
        │ - 5 roles             │
        └───────────────────────┘

No new tables created - uses existing candidate table
with product + primary_role + availability columns
```

---

## API Call Sequence

```
SEQUENCE DIAGRAM: Getting Products & Roles

Frontend                      Backend           roles.json
   │                             │                  │
   │─── Page Load ──────────────>│                  │
   │                             │                  │
   │                        fetchAllData()         │
   │                             │                  │
   │<─ getProducts('Oracle') ────│                  │
   │                             │                  │
   │                        Load roles.json ────→  │
   │                             │<── JSON Data ──│
   │                             │                  │
   │<─ Return [4 products] ──────│                  │
   │                             │                  │
   │ Display dropdown with        │                  │
   │ [SaaS, EBS, PS, JDE]        │                  │
   │                             │                  │
   │─── User selects "SaaS" ────>│                  │
   │                             │                  │
   │    getRoles('Oracle','SaaS')│                  │
   │                             │                  │
   │<─ getRoles request ─────────│                  │
   │                             │                  │
   │                        Load roles.json ────→  │
   │                             │<── JSON Data ──│
   │                             │                  │
   │<─ Return [7 SaaS roles] ────│                  │
   │                             │                  │
   │ Display role dropdown with   │                  │
   │ 7 roles for SaaS            │                  │
   │                             │                  │
   │─── User selects role + ────>│                  │
   │    fills availability       │                  │
   │                             │                  │
   │    POST /candidates/me      │                  │
   │                             │                  │
   │<─ Save successful ──────────│                  │
   │                             │                  │
   v                             v                  v
```

---

## Technology Stack

```
┌──────────────────────────────────────────────────────────────┐
│                    TECHNOLOGY STACK                          │
└──────────────────────────────────────────────────────────────┘

Frontend Layer:
┌─────────────────────────────────────────┐
│ React 18 + TypeScript                   │
│ ├── CandidateDashboard.tsx (Component)  │
│ ├── client.ts (API Client)              │
│ └── Dashboard.css (Styling)             │
│                                         │
│ HTTP Client: Axios                      │
│ State Management: React Hooks (useState)│
│ Running on: http://localhost:3000       │
└─────────────────────────────────────────┘
           ↓ API Calls ↓
Backend Layer:
┌─────────────────────────────────────────┐
│ FastAPI (Python Web Framework)          │
│ ├── job_roles.py (Router/Endpoints)     │
│ └── main.py (Application Setup)         │
│                                         │
│ Database: SQLite                        │
│ ORM: SQLModel (SQLAlchemy + Pydantic)   │
│ Running on: http://127.0.0.1:8000       │
└─────────────────────────────────────────┘
           ↓ Reads from ↓
Data Layer:
┌─────────────────────────────────────────┐
│ JSON Files                              │
│ ├── roles.json (Products & Roles)       │
│ └── skills.json (Skills Data)           │
│                                         │
│ Location: backend/app/data/             │
│ Format: Hierarchical JSON               │
│ Access: Read-only via API               │
└─────────────────────────────────────────┘
```

---

## Deployment Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                  DEPLOYMENT DIAGRAM                          │
└──────────────────────────────────────────────────────────────┘

Development Machine
┌───────────────────────────────────────────────────────┐
│                                                       │
│  Frontend (React)                Backend (FastAPI)   │
│  localhost:3000            ←→    127.0.0.1:8000     │
│  ├── npm start                    ├── uvicorn      │
│  ├── Port 3000                    ├── Port 8000    │
│  └── Hot reload                   └── Auto reload  │
│                                                       │
│  Data Layer                                           │
│  ├── roles.json                                       │
│  ├── skills.json                                      │
│  └── moblyze_poc.db (SQLite)                         │
│                                                       │
└───────────────────────────────────────────────────────┘

Both running on same machine for development
Ready to deploy to production (separate servers)
```

---

## Summary

**This diagram shows:**
1. ✅ Complete data flow from roles.json to UI
2. ✅ Component interaction and state management
3. ✅ API endpoints and their purpose
4. ✅ Database structure and schema
5. ✅ Technology stack and frameworks
6. ✅ Deployment architecture

**Key Features:**
- Data originates from single source: roles.json
- Products pre-loaded on mount
- Roles loaded dynamically based on selection
- All data flows through APIs
- Form integrates with existing profile system

